# Important Notice

### File does not contain node_modules folder
### To run the application, download the source code and then use the following command in the project directory

```cmd
  npm install
```
